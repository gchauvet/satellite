FIND_PACKAGE(JNI)

IF(JNI_FOUND)
  INCLUDE_DIRECTORIES(SYSTEM ${JNI_INCLUDE_DIRS})
ELSE()
  INCLUDE_DIRECTORIES(SYSTEM /usr/lib/jvm/default-java/include /usr/lib/jvm/default-java/include/linux)
ENDIF()

FILE(GLOB SOURCES *.c)
ADD_LIBRARY (daemon STATIC ${SOURCES})
